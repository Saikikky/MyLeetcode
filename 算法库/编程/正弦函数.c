#include <stdio.h>
#include "MyMath.h"
const double pi = 3.1415926535898; 
REAL Sin(REAL x);
int main()
{
	REAL a, s;
	printf("a = ? ");
	scanf(RSF, &a);
	s = Sin(a);
	printf("s = " RPF "\n", s);
	return 0;
}
REAL Sin(REAL x)
{
   
}
